# お気に入り機能 実装設計書

## 📋 目次

1. [機能概要](#機能概要)
2. [要件定義](#要件定義)
3. [アーキテクチャ設計](#アーキテクチャ設計)
4. [データ構造](#データ構造)
5. [UI/UXデザイン](#uiuxデザイン)
6. [実装計画](#実装計画)
7. [テスト計画](#テスト計画)
8. [パフォーマンス考慮事項](#パフォーマンス考慮事項)

---

## 機能概要

ユーザーがメニューをお気に入りとして保存し、後で簡単にアクセスできる機能。ローカルストレージを使用してブラウザにデータを保存し、ユーザー認証なしで利用可能。

### 主要機能
- ✅ メニューのお気に入り追加/削除
- ✅ お気に入り一覧ページ
- ✅ お気に入り数のバッジ表示
- ✅ お気に入り状態の永続化（localStorage）
- ✅ お気に入りメニューのエクスポート/インポート（JSON）

---

## 要件定義

### 機能要件

#### FR-1: お気に入り追加/削除
- メニューカードから1クリックでお気に入りに追加
- お気に入り済みの場合は削除可能
- リアルタイムでUI更新（アニメーション付き）
- 最大保存数: 500件（パフォーマンス考慮）

#### FR-2: お気に入り一覧表示
- 専用ページ（`/favorites`）で一覧表示
- メニュー一覧ページと同じUIコンポーネント再利用
- フィルター・検索機能対応
- ソート機能: 追加日時、価格、名前

#### FR-3: お気に入り数表示
- ヘッダーにバッジ表示
- リアルタイム更新

#### FR-4: データ永続化
- localStorage使用
- キー: `disney-menu-favorites`
- JSON形式で保存

#### FR-5: データエクスポート/インポート
- JSONファイルでエクスポート
- 他のブラウザやデバイスでインポート可能
- バックアップ目的

### 非機能要件

#### NFR-1: パフォーマンス
- お気に入り追加/削除: 100ms以内
- 一覧ページ読み込み: 300ms以内
- localStorageサイズ: 5MB以下

#### NFR-2: ユーザビリティ
- アイコンボタンで視覚的に分かりやすい
- 追加時の視覚的フィードバック（アニメーション）
- エラーハンドリング（ストレージ容量超過時）

#### NFR-3: 互換性
- ブラウザ: Chrome, Safari, Firefox, Edge
- デバイス: PC, タブレット, スマートフォン

---

## アーキテクチャ設計

### システム構成図

```
┌─────────────────────────────────────────────┐
│           React Application                 │
│                                             │
│  ┌─────────────────────────────────────┐  │
│  │      FavoritesContext               │  │
│  │  - favorites: string[]              │  │
│  │  - addFavorite()                    │  │
│  │  - removeFavorite()                 │  │
│  │  - toggleFavorite()                 │  │
│  │  - isFavorite()                     │  │
│  │  - clearAll()                       │  │
│  └──────────────┬──────────────────────┘  │
│                 │                          │
│     ┌───────────┴───────────┐             │
│     │                       │             │
│  ┌──▼──────┐         ┌─────▼───────┐     │
│  │ useFavorites  │    │ FavoritesStorage  │
│  │  Hook         │    │  Module           │
│  └───────────┘         └──────┬──────┘     │
│                              │             │
│                    ┌─────────▼──────────┐  │
│                    │  localStorage       │  │
│                    │  Key: disney-menu-  │  │
│                    │       favorites     │  │
│                    └────────────────────┘  │
└─────────────────────────────────────────────┘
```

### ディレクトリ構造

```
frontend/src/
├── contexts/
│   └── FavoritesContext.tsx       # お気に入りコンテキスト
├── hooks/
│   └── useFavorites.ts            # お気に入りフック
├── components/
│   ├── favorites/
│   │   ├── FavoriteButton.tsx     # お気に入りボタン
│   │   ├── FavoritesBadge.tsx     # お気に入り数バッジ
│   │   └── FavoritesExport.tsx    # エクスポート/インポート
│   └── menu/
│       └── MenuCard.tsx           # メニューカード（更新）
├── pages/
│   └── FavoritesPage.tsx          # お気に入り一覧ページ
├── services/
│   └── favoritesStorage.ts        # localStorage操作
└── types/
    └── favorites.ts               # 型定義
```

---

## データ構造

### localStorage構造

```typescript
// キー: disney-menu-favorites
// 値: JSON文字列
{
  "version": "1.0",
  "favorites": [
    {
      "menuId": "0123",
      "addedAt": "2026-01-01T10:30:00.000Z"
    },
    {
      "menuId": "0456",
      "addedAt": "2026-01-01T11:15:00.000Z"
    }
  ],
  "updatedAt": "2026-01-01T11:15:00.000Z"
}
```

### TypeScript型定義

```typescript
// types/favorites.ts

/**
 * お気に入りアイテム
 */
export interface FavoriteItem {
  menuId: string;
  addedAt: string; // ISO 8601形式
}

/**
 * お気に入りストレージデータ
 */
export interface FavoritesData {
  version: string;
  favorites: FavoriteItem[];
  updatedAt: string;
}

/**
 * お気に入りコンテキスト型
 */
export interface FavoritesContextType {
  favorites: string[]; // メニューIDの配列
  addFavorite: (menuId: string) => void;
  removeFavorite: (menuId: string) => void;
  toggleFavorite: (menuId: string) => void;
  isFavorite: (menuId: string) => boolean;
  clearAll: () => void;
  exportFavorites: () => string; // JSON文字列
  importFavorites: (data: string) => boolean;
  count: number;
}

/**
 * エクスポートデータ型
 */
export interface ExportData {
  version: string;
  exportedAt: string;
  favorites: FavoriteItem[];
}
```

---

## UI/UXデザイン

### 1. お気に入りボタン（FavoriteButton）

**配置場所**: メニューカード内（右上）

**デザイン**:
```
┌─────────────────────────────┐
│  Menu Card                  │
│                   [❤️/🤍]   │  ← お気に入りボタン
│  ┌─────────────────┐        │
│  │     Image       │        │
│  └─────────────────┘        │
│  Title                      │
│  Price: ¥1,200              │
└─────────────────────────────┘
```

**状態**:
- 未お気に入り: アウトラインハートアイコン（🤍）
- お気に入り済み: 塗りつぶしハートアイコン（❤️）+ 赤色

**アニメーション**:
- クリック時: スケールアニメーション（1.0 → 1.3 → 1.0）
- 追加時: ハートが弾むアニメーション

### 2. お気に入りバッジ（FavoritesBadge）

**配置場所**: ヘッダーのナビゲーション

**デザイン**:
```
[🏠 ホーム]  [❤️ お気に入り (12)]  [ℹ️ About]
                        ↑
                    バッジ表示
```

**実装**:
- Material-UI Badge コンポーネント使用
- お気に入り数が0の場合はバッジ非表示
- 最大表示数: 99+（100以上は「99+」表示）

### 3. お気に入り一覧ページ（FavoritesPage）

**レイアウト**:
```
┌────────────────────────────────────────────┐
│  Disney Menu - お気に入り                  │
├────────────────────────────────────────────┤
│  [🔍 検索] [フィルター] [ソート▼] [Export] │
├────────────────────────────────────────────┤
│                                            │
│  お気に入りメニュー (12件)                 │
│                                            │
│  ┌────────┐ ┌────────┐ ┌────────┐        │
│  │ Menu 1 │ │ Menu 2 │ │ Menu 3 │        │
│  └────────┘ └────────┘ └────────┘        │
│  ┌────────┐ ┌────────┐ ┌────────┐        │
│  │ Menu 4 │ │ Menu 5 │ │ Menu 6 │        │
│  └────────┘ └────────┘ └────────┘        │
│                                            │
│  [すべてクリア]                            │
└────────────────────────────────────────────┘
```

**空状態（お気に入り0件）**:
```
┌────────────────────────────────────────────┐
│           お気に入りがありません           │
│                                            │
│              ❤️                           │
│                                            │
│   メニューのハートアイコンをタップして     │
│     お気に入りに追加しましょう！           │
│                                            │
│         [メニュー一覧へ戻る]               │
└────────────────────────────────────────────┘
```

### 4. エクスポート/インポートダイアログ

**エクスポート**:
```
┌─────────────────────────────────────┐
│  お気に入りをエクスポート           │
├─────────────────────────────────────┤
│  お気に入りメニュー: 12件           │
│                                     │
│  [📥 JSONファイルをダウンロード]    │
│                                     │
│  ※ファイルは他のデバイスで           │
│    インポート可能です                │
│                                     │
│        [キャンセル]                 │
└─────────────────────────────────────┘
```

**インポート**:
```
┌─────────────────────────────────────┐
│  お気に入りをインポート             │
├─────────────────────────────────────┤
│  JSONファイルを選択:                │
│                                     │
│  [📄 ファイルを選択]                │
│                                     │
│  ⚠️ 既存のお気に入りは上書き        │
│     されます                        │
│                                     │
│  [インポート]  [キャンセル]         │
└─────────────────────────────────────┘
```

---

## 実装計画

### Phase 1: 基盤実装（2-3時間）

**タスク**:
1. ✅ 型定義作成（`types/favorites.ts`）
2. ✅ localStorageサービス実装（`services/favoritesStorage.ts`）
3. ✅ FavoritesContext作成（`contexts/FavoritesContext.tsx`）
4. ✅ useFavoritesフック作成（`hooks/useFavorites.ts`）
5. ✅ Appコンポーネントへのコンテキスト統合

**成果物**:
- お気に入りの追加/削除/確認ロジック
- データ永続化機能
- 全コンポーネントからアクセス可能なコンテキスト

### Phase 2: UI実装（2-3時間）

**タスク**:
1. ✅ FavoriteButtonコンポーネント作成
2. ✅ MenuCardへのFavoriteButton統合
3. ✅ FavoritesBadgeコンポーネント作成
4. ✅ ヘッダーへのバッジ統合
5. ✅ アニメーション実装（Framer Motion or CSS）

**成果物**:
- メニューカードからお気に入り操作可能
- ヘッダーにお気に入り数表示

### Phase 3: お気に入り一覧ページ（1-2時間）

**タスク**:
1. ✅ FavoritesPageコンポーネント作成
2. ✅ ルーティング設定（`/favorites`）
3. ✅ お気に入りメニューのフェッチ処理
4. ✅ 空状態UI実装
5. ✅ ソート機能実装

**成果物**:
- お気に入り一覧ページ完成
- メニュー一覧ページと同等のUI

### Phase 4: エクスポート/インポート機能（1時間）

**タスク**:
1. ✅ FavoritesExportコンポーネント作成
2. ✅ JSONエクスポート処理
3. ✅ JSONインポート処理
4. ✅ エラーハンドリング
5. ✅ バリデーション実装

**成果物**:
- JSONエクスポート/インポート機能
- データバックアップ機能

---

## テスト計画

### 単体テスト

#### 1. favoritesStorage.ts
```typescript
describe('FavoritesStorage', () => {
  beforeEach(() => {
    localStorage.clear();
  });

  test('お気に入りを追加できる', () => {
    const storage = new FavoritesStorage();
    storage.add('0123');
    expect(storage.getAll()).toContain('0123');
  });

  test('お気に入りを削除できる', () => {
    const storage = new FavoritesStorage();
    storage.add('0123');
    storage.remove('0123');
    expect(storage.getAll()).not.toContain('0123');
  });

  test('お気に入り状態を確認できる', () => {
    const storage = new FavoritesStorage();
    storage.add('0123');
    expect(storage.has('0123')).toBe(true);
    expect(storage.has('9999')).toBe(false);
  });

  test('最大500件まで保存できる', () => {
    const storage = new FavoritesStorage();
    for (let i = 0; i < 501; i++) {
      storage.add(String(i).padStart(4, '0'));
    }
    expect(storage.getAll().length).toBe(500);
  });
});
```

#### 2. useFavorites.ts
```typescript
describe('useFavorites Hook', () => {
  test('お気に入りを追加できる', () => {
    const { result } = renderHook(() => useFavorites());
    
    act(() => {
      result.current.addFavorite('0123');
    });
    
    expect(result.current.isFavorite('0123')).toBe(true);
    expect(result.current.count).toBe(1);
  });

  test('トグル機能が正常に動作する', () => {
    const { result } = renderHook(() => useFavorites());
    
    act(() => {
      result.current.toggleFavorite('0123');
    });
    expect(result.current.isFavorite('0123')).toBe(true);
    
    act(() => {
      result.current.toggleFavorite('0123');
    });
    expect(result.current.isFavorite('0123')).toBe(false);
  });
});
```

### コンポーネントテスト

#### 1. FavoriteButton.tsx
```typescript
describe('FavoriteButton', () => {
  test('お気に入りでない場合、アウトラインアイコンを表示', () => {
    render(<FavoriteButton menuId="0123" />);
    expect(screen.getByTestId('favorite-outline')).toBeInTheDocument();
  });

  test('クリックでお気に入りに追加', async () => {
    render(<FavoriteButton menuId="0123" />);
    const button = screen.getByRole('button');
    
    await userEvent.click(button);
    
    expect(screen.getByTestId('favorite-filled')).toBeInTheDocument();
  });
});
```

#### 2. FavoritesPage.tsx
```typescript
describe('FavoritesPage', () => {
  test('お気に入りメニューを表示', () => {
    // お気に入りに2件追加した状態でレンダリング
    render(<FavoritesPage />);
    expect(screen.getByText(/2件/)).toBeInTheDocument();
  });

  test('お気に入り0件の場合、空状態を表示', () => {
    render(<FavoritesPage />);
    expect(screen.getByText(/お気に入りがありません/)).toBeInTheDocument();
  });
});
```

### E2Eテスト（Playwright）

```typescript
test('お気に入り機能の完全フロー', async ({ page }) => {
  await page.goto('http://localhost:5173');

  // メニューカードのお気に入りボタンをクリック
  await page.click('[data-testid="favorite-button-0123"]');

  // バッジ表示確認
  await expect(page.locator('[data-testid="favorites-badge"]')).toHaveText('1');

  // お気に入りページへ移動
  await page.click('a[href="/favorites"]');

  // お気に入りメニューが表示されていることを確認
  await expect(page.locator('[data-menu-id="0123"]')).toBeVisible();

  // お気に入りから削除
  await page.click('[data-testid="favorite-button-0123"]');

  // 空状態が表示されることを確認
  await expect(page.locator('text=お気に入りがありません')).toBeVisible();
});
```

---

## パフォーマンス考慮事項

### 1. localStorage最適化

**問題**: 頻繁な読み書きがパフォーマンスに影響

**解決策**:
- メモリキャッシュ使用（FavoritesContext内）
- localStorageへの書き込みはdebounce（500ms）
- 読み込みは初回のみ、以降はメモリから取得

```typescript
// 例: デバウンス処理
const debouncedSave = useCallback(
  debounce((favorites: string[]) => {
    FavoritesStorage.save(favorites);
  }, 500),
  []
);
```

### 2. 大量データ対応

**制限**:
- 最大500件まで保存
- 500件超過時は古いものから削除
- 警告メッセージ表示

**メモリ使用量**:
- 1件あたり約50バイト
- 500件で約25KB（localStorageは5MB制限）

### 3. レンダリング最適化

**React.memo使用**:
```typescript
export const FavoriteButton = memo<FavoriteButtonProps>(({ menuId }) => {
  // ...
});
```

**useMemoでお気に入りメニューをキャッシュ**:
```typescript
const favoriteMenus = useMemo(() => {
  return menus.filter(menu => favorites.includes(menu.id));
}, [menus, favorites]);
```

---

## セキュリティ考慮事項

### 1. XSS対策
- localStorageから取得したデータを必ずバリデーション
- JSONパース時のエラーハンドリング

### 2. データ検証
```typescript
function validateFavoritesData(data: any): data is FavoritesData {
  return (
    typeof data === 'object' &&
    typeof data.version === 'string' &&
    Array.isArray(data.favorites) &&
    data.favorites.every(
      (item: any) =>
        typeof item.menuId === 'string' &&
        typeof item.addedAt === 'string'
    )
  );
}
```

---

## エラーハンドリング

### 1. localStorage容量超過

```typescript
try {
  localStorage.setItem(key, JSON.stringify(data));
} catch (error) {
  if (error instanceof DOMException && error.name === 'QuotaExceededError') {
    // 古いお気に入りを削除
    const trimmedFavorites = favorites.slice(-400); // 最新400件のみ保持
    localStorage.setItem(key, JSON.stringify(trimmedFavorites));
    
    // ユーザーに通知
    showNotification('お気に入りの保存容量が上限に達しました。古いデータを削除しました。');
  }
}
```

### 2. インポートエラー

```typescript
try {
  const data = JSON.parse(jsonString);
  if (!validateFavoritesData(data)) {
    throw new Error('Invalid data format');
  }
  importFavorites(data);
} catch (error) {
  showErrorDialog('無効なファイル形式です。正しいお気に入りファイルを選択してください。');
}
```

---

## 今後の拡張性

### Phase 2: サーバー同期（将来）
- ユーザー認証機能追加時
- FirebaseやSupabaseで同期
- 複数デバイス間でお気に入り共有

### Phase 3: ソーシャル機能
- お気に入りメニューをSNSでシェア
- 友達のお気に入りを表示
- 人気のお気に入りメニューランキング

---

## 実装チェックリスト

### Phase 1: 基盤実装
- [ ] `types/favorites.ts` 作成
- [ ] `services/favoritesStorage.ts` 実装
- [ ] `contexts/FavoritesContext.tsx` 作成
- [ ] `hooks/useFavorites.ts` 作成
- [ ] `App.tsx` にコンテキスト統合
- [ ] 単体テスト作成

### Phase 2: UI実装
- [ ] `components/favorites/FavoriteButton.tsx` 作成
- [ ] `MenuCard.tsx` 更新
- [ ] `components/favorites/FavoritesBadge.tsx` 作成
- [ ] ヘッダーにバッジ追加
- [ ] アニメーション実装
- [ ] コンポーネントテスト作成

### Phase 3: お気に入り一覧ページ
- [ ] `pages/FavoritesPage.tsx` 作成
- [ ] ルーティング設定
- [ ] 空状態UI実装
- [ ] ソート機能実装
- [ ] E2Eテスト作成

### Phase 4: エクスポート/インポート
- [ ] `components/favorites/FavoritesExport.tsx` 作成
- [ ] エクスポート機能実装
- [ ] インポート機能実装
- [ ] バリデーション実装
- [ ] 統合テスト作成

### 最終確認
- [ ] すべてのテストがパス
- [ ] E2Eテストが成功
- [ ] パフォーマンステスト実施
- [ ] ドキュメント更新
- [ ] README更新

---

## 参考資料

- [React Context API](https://react.dev/reference/react/createContext)
- [localStorage API](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage)
- [Material-UI Icons](https://mui.com/material-ui/material-icons/)
- [Framer Motion](https://www.framer.com/motion/) (アニメーション用)

---

## 見積もり時間

| フェーズ | 作業内容 | 見積時間 |
|---------|---------|---------|
| Phase 1 | 基盤実装 | 2-3時間 |
| Phase 2 | UI実装 | 2-3時間 |
| Phase 3 | お気に入り一覧 | 1-2時間 |
| Phase 4 | エクスポート/インポート | 1時間 |
| テスト作成 | 単体・統合・E2E | 1-2時間 |
| **合計** | | **7-11時間** |

---

作成日: 2026年1月1日
バージョン: 1.0
作成者: GitHub Copilot
