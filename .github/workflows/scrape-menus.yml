name: Weekly Menu Scraping

on:
  # 毎週日曜日の午前3時（JST）に実行
  schedule:
    - cron: '0 18 * * 0'  # UTC 18:00 = JST 3:00

  # 手動実行も可能
  workflow_dispatch:
    inputs:
      start_id:
        description: '開始ID（デフォルト: 0）'
        required: false
        default: '0'
      end_id:
        description: '終了ID（デフォルト: 9999）'
        required: false
        default: '9999'

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run scraping script
        env:
          START_ID: ${{ github.event.inputs.start_id || '0' }}
          END_ID: ${{ github.event.inputs.end_id || '9999' }}
        run: |
          python scripts/scrape_menus_simple.py \
            --start ${START_ID} \
            --end ${END_ID} \
            --output data/menus.json \
            --rate-limit 1.0

      - name: Apply category assignments
        run: |
          echo "📁 カテゴリを自動割り当て中..."
          python scripts/assign_categories.py data/menus.json

      - name: Clean and normalize tags
        run: |
          echo "🏷️  タグを正規化中..."
          python scripts/clean_tags.py data/menus.json

      - name: Categorize food types
        run: |
          echo "🍽️  料理種類タグを付与中..."
          python scripts/categorize_food_types.py --input data/menus.json

      - name: Validate processed data
        run: |
          echo "✓ データを検証中..."
          python scripts/validate_menus.py data/menus.json

      - name: Check for changes
        id: git-check
        run: |
          git diff --exit-code data/menus.json || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # メニュー数と統計を取得
          MENU_COUNT=$(python -c "import json; print(len(json.load(open('data/menus.json'))))")

          # 料理種類タグの統計
          FOOD_TAG_STATS=$(python -c "
import json
with open('data/menus.json') as f:
    menus = json.load(f)
food_tags = ['麺類', 'ご飯もの', 'パン', '肉料理', '魚介料理', 'ピザ', 'デザート']
stats = []
for tag in food_tags:
    count = sum(1 for m in menus if tag in m.get('tags', []))
    if count > 0:
        stats.append(f'{tag}:{count}')
print(', '.join(stats[:5]))  # 上位5つ
")

          git add data/menus.json
          git commit -m "chore: 週次メニューデータ更新

- スクレイピング実行日時: $(date -u +'%Y-%m-%d %H:%M:%S UTC')
- 総メニュー数: ${MENU_COUNT}件
- 料理種類タグ: ${FOOD_TAG_STATS}
- 処理: スクレイピング → カテゴリ割当 → タグ正規化 → 料理種類タグ付与
- 自動実行: GitHub Actions"

          git push

      - name: Deploy to Vercel
        if: steps.git-check.outputs.changed == 'true'
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          # Vercel CLIをインストール
          npm install -g vercel

          # 本番環境にデプロイ
          echo "🚀 Vercelへデプロイ中..."
          vercel --prod --yes \
            --token ${VERCEL_TOKEN} \
            || echo "⚠️  Vercelデプロイをスキップ（Secretsが未設定の可能性）"

      - name: Create summary
        if: always()
        run: |
          echo "## 🍴 スクレイピング実行結果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ -f data/menus.json ]; then
            MENU_COUNT=$(python -c "import json; menus=json.load(open('data/menus.json')); print(len(menus))")
            echo "✅ **総メニュー数**: ${MENU_COUNT}件" >> $GITHUB_STEP_SUMMARY

            # 詳細統計
            python << 'EOF' >> $GITHUB_STEP_SUMMARY
import json
from collections import Counter

with open('data/menus.json') as f:
    menus = json.load(f)

# パーク別
tdl_count = sum(1 for m in menus if any(r.get('park') == 'tdl' for r in m.get('restaurants', [])))
tds_count = sum(1 for m in menus if any(r.get('park') == 'tds' for r in m.get('restaurants', [])))

print(f"\n### 📊 統計情報")
print(f"- **ディズニーランド**: {tdl_count}件")
print(f"- **ディズニーシー**: {tds_count}件")

# 料理種類タグ統計
food_tags = ['麺類', 'ご飯もの', 'パン', '肉料理', '魚介料理', 'ピザ', 'スープ', 'デザート', 'アイスクリーム']
food_stats = []
for tag in food_tags:
    count = sum(1 for m in menus if tag in m.get('tags', []))
    if count > 0:
        food_stats.append((tag, count))

if food_stats:
    print(f"\n### 🍽️ 料理種類タグ（上位5件）")
    for tag, count in sorted(food_stats, key=lambda x: x[1], reverse=True)[:5]:
        print(f"- {tag}: {count}件")
EOF

            echo "" >> $GITHUB_STEP_SUMMARY
            if [ "${{ steps.git-check.outputs.changed }}" == "true" ]; then
              echo "### 🔄 実行内容" >> $GITHUB_STEP_SUMMARY
              echo "1. ✅ メニュースクレイピング" >> $GITHUB_STEP_SUMMARY
              echo "2. ✅ カテゴリ自動割り当て" >> $GITHUB_STEP_SUMMARY
              echo "3. ✅ タグ正規化" >> $GITHUB_STEP_SUMMARY
              echo "4. ✅ 料理種類タグ付与" >> $GITHUB_STEP_SUMMARY
              echo "5. ✅ データ検証" >> $GITHUB_STEP_SUMMARY
              echo "6. ✅ Gitコミット・プッシュ" >> $GITHUB_STEP_SUMMARY
              echo "7. 🚀 Vercel本番デプロイ" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "🎉 **データが更新され、本番環境にデプロイされました！**" >> $GITHUB_STEP_SUMMARY
            else
              echo "ℹ️ データに変更はありませんでした" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "❌ スクレイピングに失敗しました" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload artifact on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scraping-logs
          path: |
            data/menus.json
            *.log
          retention-days: 7
